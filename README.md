# osteo-methods

Methods study on a matched case-control dataset: what happens when
standard supervised ML is applied to a design that assumes conditional
likelihood.

Data are not included — see `data/README.md`.
